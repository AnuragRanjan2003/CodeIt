import 'package:flutter/material.dart';

class ProfileScreenPortrait extends StatelessWidget {
  const ProfileScreenPortrait({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
