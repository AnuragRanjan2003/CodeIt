import 'package:flutter/material.dart';

class AddAccountsLandscape extends StatelessWidget {
  final BoxConstraints cons;
  const AddAccountsLandscape({super.key, required this.cons});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(backgroundColor: Colors.orange,);
  }
}
